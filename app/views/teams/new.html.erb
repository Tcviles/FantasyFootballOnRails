<%=form_for(@team) do |f|%>
  <%= f.label :name%>
  <%= f.text_field :name%>
  <%= f.label :player%>
  <%= binding.pry%>
  <%= f.collection_select(:player_ids, Player.all, :id, :name) %>
  <%= f.submit %>
<%end%>
