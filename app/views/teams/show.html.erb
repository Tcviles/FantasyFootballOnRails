<label class="title"><%= @team.user.email%></label><br>
<label style="font-size:150%;"><%= @team.name %></label>

<table>
  <thead>
    <th>Name</th>
    <th>NFL</th>
    <th>Position</th>
  </thead>
  <tbody>
    <% @team.players.each do |player|%>
    <tr>
      <td><%= player.name%></td>
      <td><%= player.nfl_team%></td>
      <td><%= player.position.name%></td>
    </tr>
    <%end%>
  </tbody>
</table>
<%= link_to 'Back', @team.league %>
<% if @team.user == current_user %>
  <%= link_to 'Delete Team', @team, method: :delete, data: { confirm: 'Are you sure??' } %>
  <%= link_to 'Edit Team', edit_league_team_path(params[:league_id],@team) %>
<%end%>
